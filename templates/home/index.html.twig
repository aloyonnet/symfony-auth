{% extends 'base.html.twig' %}

{% block title %}Hello HomeController!{% endblock %}

{% block body %}
<style>
    .example-wrapper { margin: 1em auto; max-width: 800px; width: 95%; font: 18px/1.5 sans-serif; }
    .example-wrapper code { background: #F5F5F5; padding: 2px 6px; }
</style>

{% if app.user %}
    <div class="card">
    <h5 class="card-header">You are logged in</h5>
    <div class="card-body">
        <h5 class="card-title">Welcome {{ app.user.username }}</h5>
        <p class="card-text">You have been able to log yourself thanks to the Symfony User system</p>
        <a href="{{ path('logout') }}" class="btn btn-primary">Logout</a>
    </div>
    </div>
{% endif %}
<div class="example-wrapper">
    <h1>Hello {{ controller_name }}! ✅</h1>

    This friendly message is coming from:
    <ul>
        <li>Your controller at <code>/home/reol/www/symfony-auth/src/Controller/HomeController.php</code></li>
        <li>Your template at <code>/home/reol/www/symfony-auth/templates/home/index.html.twig</code></li>
    </ul>
</div>
{% endblock %}
